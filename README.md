Prerequisites:
Arm GNU Toolchain (arm-none-eabi-gcc, arm-none-eabi-gdb)
CMake
Ninja (avoids incorrectly building with MSVC especially if you have Visual Studio installed)
STM32CubeProgrammer / ST-LINK GDB Server (for debugging)
